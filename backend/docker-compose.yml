services:

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: kharcha
    ports:
      - "5432:5432"
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env